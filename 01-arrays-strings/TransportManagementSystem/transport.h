#include <stdio.h>
#include "transport.h"

int vehicleID[MAX];
char vehicleName[MAX][30];
char route[MAX][50];
float fare[MAX];
int passengers[MAX];
int count = 0;

int main() {
    // We'll call our functions here as we build them
    return 0;
}